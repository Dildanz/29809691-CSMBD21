def combiner(counts):
    """
    Combiner aggregates the flight counts for each passenger

    Parameters
    counts - A list of flight counts for a passenger

    Returns
    int - The sum of the flight counts
    """
    return sum(counts)